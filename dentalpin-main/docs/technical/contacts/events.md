---
module: contacts
last_verified_commit: 0a0651a
---

# contacts — events

None. The module neither publishes nor consumes events. If a future
`lab_orders` module needs to react to contact changes (e.g. a lab being
deactivated), add `contact.*` events here first.
