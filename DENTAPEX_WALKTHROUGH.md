# دليل التوثيق الشامل لنظام دنت أبيكس: DentApex Walkthrough
**تاريخ التوثيق**: 2026-09-19  
**حالة النظام**: نشط ويعمل محلياً بالكامل (Native Windows Portable - 100% Operational)

---

## 1. الفلسفة المعمارية والقيود الصارمة (Architecture & Constraints)

1. **العمل المحلي المحمول بدون حاويات (Zero Docker / No WSL2)**:
   - النظام يعمل بالكامل بنسخ محمولة (Portable Binaries) على Windows مباشرة.
   - الهدف الأساسي: أعلى أداء ممكن وأقل استهلاك للموارد للعمل بسلاسة تامة على أضعف حواسيب العيادات (مثل Core i3 قديم، ذاكرة 4GB RAM).
2. **ميزانية الذاكرة الصارمة (RAM Budget)**:
   - المستهدف المعماري: خادم الباك إند + قاعدة البيانات <= 120 ميجابايت.
   - المستهدف لكامل النظام مع السيرفر: <= 150 ميجابايت.
   - **المحقق فعلياً**: الباك إند وقاعدة البيانات معاً **91.94 ميجابايت**، والنظام بالكامل مع خادم Caddy **126.13 ميجابايت**.
3. **أمان قاعدة البيانات (Cryptographic Security)**:
   - تشفير كلمات المرور والاتصال محلياً حصرياً بـ `scram-sha-256` عبر دور `dentapex_admin`.
   - منع استخدام أي وضع غير آمن (`trust` mode ممنوع نهائياً).
4. **الذكاء الاصطناعي (AI Copilot)**:
   - التعرف الكامل على الهوية الجديدة **دنت أبيكس (DentApex)** دون أدنى أثر للهوية القديمة.
   - التفاعل الطبي والإداري الاحترافي باللغة العربية الفصحى مع الفهم التام للعامية المصرية ومختلف اللهجات.
5. **توليد مستندات الـ PDF**:
   - الاعتماد على محرك Microsoft Edge Headless المدمج في ويندوز مع الحفاظ التام على هوية DentApex في التذييل والعناوين بدون مكتبات ثقيلة مثل WeasyPrint أو أدوات خارجية تتطلب متطلبات معقدة.

---

## 2. جدول الخدمات والمنافذ الحالية (Services & Ports)

| الخدمة | التقنية والمشغل | المنفذ (Port) | المسار المحلي | الحالة |
| :--- | :--- | :---: | :--- | :---: |
| **قاعدة البيانات** | PostgreSQL 16 Portable | `5432` | `bin\postgresql-portable\pgsql\bin\postgres.exe` | **يعمل** |
| **خادم الباك إند** | Python 3.11 FastAPI (Uvicorn) | `7071` | `dentalpin-main\backend\venv\Scripts\python.exe` | **يعمل** |
| **سيرفر الواجهة والبروكسي** | Caddy 2.8 Portable | `7070` | `bin\caddy.exe` | **يعمل** |
| **الواجهة الأمامية** | Nuxt 3 SPA (Pre-rendered) | `7070` | تخدم مباشرة عبر Caddy من `dentalpin-main\frontend\.output\public` | **يعمل** |

- **رابط الواجهة الرئيسي للمستخدم**: [http://localhost:7070](http://localhost:7070)
- **رابط فحص صحة الـ API المباشر**: [http://localhost:7071/api/v1](http://localhost:7071/api/v1) (يرجع: `{"message": "DentApex API", "version": "2.0.0"}`)

---

## 3. تفاصيل التعديلات وإعادة التسمية (Rebranding & White-Labeling)

### أ. قاعدة البيانات (Database Layer):
- **اسم القاعدة**: تم تغيير الاسم من `dentalpin_db` إلى **`dentapex_db`**.
- **المستخدم وأمان الاتصال**:
  - إنشاء Superuser باسم `dentapex_admin`.
  - كلمة المرور: مشفرة بالكامل بـ `scram-sha-256`.
  - تم تعطيل حساب `dentalpin_admin` القديم وإلغاء إمكانية دخوله (`NOLOGIN`) وإعادة تسميته إلى `_legacy_admin`.
- **سلامة البيانات**:
  - تم فحص وتأكيد سلامة **114 جدولاً**.
  - **129 إجراء سني معرب** بالكامل لطب وجراحة الأسنان متوفرة في دليل الخدمات السنية (`procedures_catalog`).
  - بيانات الفروع، المستخدمين، والصلاحيات لم تفقد أي قيمة.

### ب. الباك إند والـ Environment (`backend/`):
- **ملف الإعدادات البيئية (`.env`)**:
  - `DATABASE_URL=postgresql+asyncpg://dentapex_admin:***@127.0.0.1:5432/dentapex_db`
  - `DB_USER=dentapex_admin`
  - `DB_NAME=dentapex_db`
  - `SECRET_KEY=dentapex_arabic_secret_key_...`
  - `BUDGET_PUBLIC_SECRET_KEY=dentapex_budget_secret_key_...`
  - `DENTAPEX_DEV_MODULE_SCAN=true`
- **نواة التطبيق والـ CLI**:
  - `app/main.py`: العنوان الافتراضي `title="DentApex API"`.
  - `app/config.py`: منشئ النظام الافتراضي `DentApex`، والبريد `noreply@dentapex.com`.
  - `app/core/plugins/loader.py`: دعم مسار الحزم `dentapex.modules` بالإضافة إلى مسار التوافقية العكسية.
  - أوامر سطر الأوامر (CLI): توثيق ونصوص الأوامر أصبحت `dentapex db ...` و `dentapex modules ...`.
  - تذييل الـ PDF: تم تعيين التذييل في `billing/pdf.py` و `budget/pdf.py` ليطبع *«تم الإنشاء بواسطة DentApex»*.
  - إعدادات التقويم والـ ICS: تم تعديل معرّف المنظمة إلى `PRODID = "-//DentApex//Agenda//EN"` والنطاق إلى `UID_DOMAIN = "dentapex.com"`.

### ج. الذكاء الاصطناعي (AI Copilot Engine):
- **تحديث الـ System Prompt**:
  - ملف `app/modules/copilot/bridge.py`:
    > *"أنت المساعد الذكي لنظام دنت أبيكس (DentApex)، مساعد لإدارة عيادة الأسنان..."*
  - تم ربط السياق التفاعلي الحي بحيث يقوم النظام بحقن اسم العيادة الحالية، توقيتها المحلي، فروعها، وأطبائها النشطين ديناميكياً.
- **الاختبار والتحقق الحي (Live Test)**:
  - تم إرسال طلب استعلام حي إلى محرك الذكاء الاصطناعي عبر نقطة الـ API المتدفقة (SSE Stream).
  - رد المساعد الذكي بنص صريح:
    > **"مرحباً! أنت تتفاعل مع نظام دنت أبيكس (DentApex)، وهو نظام إدارة متكامل لعيادات الأسنان..."**
  - تم التأكد البرمجي من أن الرد يحتوي على `DentApex` وخالٍ تماماً من أي ذكر لـ `DentalPin`.

### د. الواجهة الأمامية والأصول البصرية (Frontend & Branding):
- **اللوجو (Logo)**:
  - **الأيقونة الهندسية البصرية للسن**: بقيت كما هي تماماً وفقاً للتوجيه الصارم لطلبك.
  - **النص البصري المصاحب (Wordmark)**: تم تعديل ملف الـ SVG `logo-horizontal.svg` ليرسم كلمة **DentApex** بنقاء ودقة تامة (`Dent` + `Apex`) مع تحديث سمة الوصول `aria-label="DentApex"`.
- **العناوين والنصوص**:
  - `nuxt.config.ts`: `title: 'DentApex'`.
  - `app/app.vue`: `DentApex - النسخة العربية الذكية`.
  - الشريط الجانبي والقوائم في القوالب (`layouts/default.vue`, `layouts/public.vue`) تم تحديثها بالكامل.
- **ملفات اللغات والترجمة (i18n)**:
  - تنقيح شامل لجميع ملفات الترجمة في `frontend/i18n/locales/` (العربية، الإنجليزية، وباقي اللغات) واستبدال أي ظهور للاسم القديم.
  - تنقيح ملفات الترجمة في الموديولات المستقلة (`india_gst`, `migration_import`, `verifactu`).
- **الحزمة الجاهزة للنشر (`.output/public`)**:
  - تم تنقيح 79 ملفاً برمجياً جاهزاً ومبسطاً (HTML, Chunks, SVGs) لتظهر الهوية الجديدة في المتصفح فوراً دون استهلاك وقت أو موارد في إعادة بناء الحزمة (Rebuild).

---

## 4. ملفات التشغيل والتحكم (Launchers & Automation)

تم إعداد المشغلات في المجلد الرئيسي لتوفير تجربة تشغيل بلمسة واحدة:

| الملف | الوظيفة وطريقة العمل |
| :--- | :--- |
| **`DentApex-Launcher.vbs`** | **المشغل الموصى به**: يشغّل كافة الخدمات في الخلفية بضغطة واحدة دون ظهور أي نوافذ موجه أوامر سوداء مزعجة، ثم يفتح المتصفح تلقائياً على `http://localhost:7070`. |
| **`DentApex.bat`** | مشغل الدفعة المباشر: يقوم بتشغيل الخدمات مع إظهار المخرجات في نافذة موجه الأوامر لمتابعة السجلات اللحظية. |
| **`DentApex-Stop.bat`** | إيقاف شامل وفوري لكافة خدمات النظام (Caddy, Python/Uvicorn, PostgreSQL) بأمان تام وبضغطة زر واحدة. |
| `bin\run_services.bat` | السكربت الداخلي المنظم لإقلاع الخدمات بالتسلسل الصحيح (قاعدة البيانات ← الباك إند ← السيرفر). |
| `bin\stop_services.bat` | السكربت الداخلي لإيقاف العمليات استناداً للمنافذ والمسارات. |

> **ملاحظة توافقية**: تم تحديث ملفات `DentalPin.bat` و `DentalPin-Launcher.vbs` القديمة لتقوم بتوجيه التشغيل تلقائياً إلى مشغلات `DentApex` الجديدة.

---

## 5. تدقيق الذاكرة والأداء (Live RAM Consumption Audit)

تم قياس استهلاك الذاكرة عبر أداة فحص دقيقة للعمليات النشطة الحالية على الجهاز:

```text
============================================================
              DENTAPEX PROCESS RAM AUDIT
============================================================
  postgres.exe (Multiple Workers)  :  74.81 MB
  python.exe   (FastAPI Backend)   :  17.13 MB
  ----------------------------------------------------------
  TOTAL (Backend + PostgreSQL)     :  91.94 MB  [Target: <= 120 MB] -> PASSED
  ----------------------------------------------------------
  caddy.exe    (Web Server/Proxy)  :  34.19 MB
  ==========================================================
  TOTAL SYSTEM MEMORY FOOTPRINT    : 126.13 MB  [Target: <= 150 MB] -> PASSED
============================================================
```

---

## 6. إجراءات النسخ الاحتياطي والصيانة الدورية (Maintenance & Backup)

- **النسخ الاحتياطي السريع لقاعدة البيانات**:
  ```powershell
  & "bin\postgresql-portable\pgsql\bin\pg_dump.exe" -U dentapex_admin -h 127.0.0.1 -p 5432 -F c -b -v -f "data\backup_dentapex_%date:~-4,4%%date:~-7,2%%date:~-10,2%.dump" dentapex_db
  ```
- **استعادة نسخة احتياطية**:
  ```powershell
  & "bin\postgresql-portable\pgsql\bin\pg_restore.exe" -U dentapex_admin -h 127.0.0.1 -p 5432 -d dentapex_db -v "data\backup_dentapex_YYYYMMDD.dump"
  ```
- **سجلات الأخطاء والتشغيل**:
  - سجل قاعدة البيانات: `data\postgres.log`
  - مخرجات الـ Backend و Caddy تتم إدارتها تلقائياً بواسطة مشغلات `bin\run_services.bat`.

---
*تم إنشاء هذا الدليل ليكون المرجع الفني الدائم والمستقر لفريق عمل ومشغلي نظام **DentApex Arabic Edition**.*
